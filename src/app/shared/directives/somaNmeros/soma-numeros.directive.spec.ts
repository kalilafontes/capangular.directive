import { SomaNumerosDirective } from './soma-numeros.directive';

describe('SomaNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new SomaNumerosDirective();
    expect(directive).toBeTruthy();
  });
});
